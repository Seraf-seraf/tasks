# Environment overrides are optional for local development.
# The Makefile uses config/config.yaml by default; use these variables only when
# you need to override values without editing YAML.

HTTP_ADDR=:8080
MYSQL_DSN=task:task@tcp(localhost:3306)/tasks?parseTime=true&multiStatements=true
REDIS_ADDR=localhost:6379
REDIS_DB=0
JWT_SECRET=change-me
